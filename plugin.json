{
  "tesserae_compat": "1.x",
  "name": "Calendar, Schedule",
  "version": "0.5.1",
  "kind": "widget",
  "description": "Timeline-rail agenda view. Lists upcoming events grouped by day, with a thin ink rail connecting per-event nodes and a coloured start-time chip carrying the feed accent. Titles wrap freely so the widget stays legible from 1 up to 4 narrow columns on wide e-ink panels. Pulls from the same calendar_core feeds as the other calendar_* widgets.",
  "icon": "ph-list-bullets",
  "supports": {
    "sizes": [
      "xs",
      "sm",
      "md",
      "lg"
    ]
  },
  "cell_options": [
    {
      "name": "days_ahead",
      "type": "select",
      "label": "Days to show",
      "default": "5",
      "choices": [
        {
          "value": "1",
          "label": "Today only"
        },
        {
          "value": "3",
          "label": "Today + 2 days"
        },
        {
          "value": "5",
          "label": "Today + 4 days (5 total)"
        },
        {
          "value": "7",
          "label": "A week"
        },
        {
          "value": "14",
          "label": "Two weeks"
        },
        {
          "value": "30",
          "label": "A month (30 days)"
        },
        {
          "value": "fill",
          "label": "Fill the cell (up to 3 months of events)"
        }
      ]
    },
    {
      "name": "feeds_filter",
      "type": "string",
      "label": "Feed IDs (comma-separated; blank = all enabled)",
      "default": "",
      "help": "Restrict to specific feeds. Leave blank to include every enabled feed configured in calendar_core."
    },
    {
      "name": "hide_keywords",
      "type": "string",
      "label": "Hide events matching (comma-separated)",
      "default": "",
      "help": "Drop events whose title or location contains any of these words. Case-insensitive, matches part of a word, so \"lunch\" hides \"Lunch with Poppy\". Handy for a recurring event you don't need on the panel."
    },
    {
      "name": "only_keywords",
      "type": "string",
      "label": "Only show events matching (comma-separated)",
      "default": "",
      "help": "Leave blank for everything. When set, only events whose title or location contains one of these words are shown; any Hide words still apply on top."
    },
    {
      "name": "skip_empty_days",
      "type": "boolean",
      "label": "Skip days with no events",
      "default": true
    },
    {
      "name": "max_events_per_day",
      "type": "number",
      "label": "Max events per day (0 = all)",
      "default": 0
    },
    {
      "name": "max_events_total",
      "type": "number",
      "label": "Max events across the whole agenda (0 = no cap)",
      "default": 0,
      "min": 0,
      "max": 200,
      "help": "Caps the total events shown across all days, after sorting chronologically. Stops a single busy day shrinking every row's text to fit. Applied after the per-day cap."
    },
    {
      "name": "columns",
      "type": "select",
      "label": "Layout columns",
      "default": "auto",
      "help": "Flow the agenda across multiple columns so a longer window (say two weeks) fits in a half-height cell without shrinking every row. Auto grows columns 1 up to 4 until the whole list fits. Days stay atomic (never split across columns), and column-fill packs by real content height, not by day count.",
      "choices": [
        {"value": "auto", "label": "Auto (grow until list fits)"},
        {"value": "1", "label": "1 column"},
        {"value": "2", "label": "2 columns (good on 1200-wide portrait)"},
        {"value": "3", "label": "3 columns (good on 1600+ wide landscape)"},
        {"value": "4", "label": "4 columns (wide panels only)"}
      ]
    },
    {
      "name": "show_title",
      "type": "boolean",
      "label": "Show widget title",
      "default": true,
      "help": "Renders a 'Schedule' header row at the top so the cell matches the Spectra widget chrome. Off uses the entire cell for the agenda."
    },
    {
      "name": "show_location",
      "type": "boolean",
      "label": "Show event locations",
      "default": true
    },
    {
      "name": "show_dot_color",
      "type": "boolean",
      "label": "Show per-feed colour dot",
      "default": true,
      "help": "Off makes the layout pure typography; useful on 1-bit panels."
    },
    {
      "name": "time_format",
      "type": "select",
      "label": "Time format",
      "default": "12h",
      "choices": [
        {
          "value": "12h",
          "label": "12-hour (10:00am)"
        },
        {
          "value": "24h",
          "label": "24-hour (10:00)"
        }
      ]
    },
    {
      "name": "date_label_style",
      "type": "select",
      "label": "Weekday / month label style",
      "default": "short",
      "choices": [
        {"value": "full", "label": "Full word (Monday, January)"},
        {"value": "short", "label": "Short (MON, JAN)"},
        {"value": "minimal", "label": "Minimal (M, JA)"}
      ]
    },
    {
      "name": "title_scale",
      "type": "slider",
      "label": "Dashboard title size",
      "default": 1.0,
      "min": 0.01,
      "max": 10.0,
      "step": 0.05,
      "unit": "×",
      "help": "Multiplier on the 'Schedule' header row at the top of the widget."
    },
    {
      "name": "header_scale",
      "type": "slider",
      "label": "Day header size",
      "default": 1.0,
      "min": 0.01,
      "max": 10.0,
      "step": 0.05,
      "unit": "×",
      "help": "Multiplier on each day's date/weekday/month header text."
    },
    {
      "name": "event_title_scale",
      "type": "slider",
      "label": "Event title size",
      "default": 1.0,
      "min": 0.01,
      "max": 10.0,
      "step": 0.05,
      "unit": "×",
      "help": "Multiplier on the event title font size. 1.0 = default. Raise for more legible titles at the cost of visible event count."
    },
    {
      "name": "event_time_scale",
      "type": "slider",
      "label": "Event time size",
      "default": 1.0,
      "min": 0.01,
      "max": 10.0,
      "step": 0.05,
      "unit": "×",
      "help": "Multiplier on the start-time chip font size."
    },
    {
      "name": "event_location_scale",
      "type": "slider",
      "label": "Event location size",
      "default": 0.9,
      "min": 0.01,
      "max": 10.0,
      "step": 0.05,
      "unit": "×",
      "help": "Multiplier on the location string beneath each event title."
    },
    {
      "name": "day_row_padding_em",
      "type": "slider",
      "label": "Day row spacing",
      "default": 0.5,
      "min": 0.0,
      "max": 3.0,
      "step": 0.05,
      "unit": "em",
      "help": "Vertical padding above + below each day block, in em units so it scales with the auto-fit font size. Lower packs more events on the panel."
    }
  ],
  "render": {
    "dither": "none",
    "needs_network": true
  }
}
